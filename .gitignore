.venv/
.env
.env.tmp
__pycache__/
*.py[cod]
*.egg-info/

# Local agent/editor config
.claude/
.codex/
.agents/
AGENTS.md

# data/ is populated by scripts/fetch_data.sh (datasets) and
# scripts/install_giella_artifacts.sh (Giella models); nothing in it is tracked.
data/
*.sqlite-shm
*.sqlite-wal

# Build/release scratch: Giella source-build cache and the archives that
# `make package-data` / `make package-giella` create in the repo root.
.cache/
/vro-data.tar.xz
/giella-share.tar.xz
